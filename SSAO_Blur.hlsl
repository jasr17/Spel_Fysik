Texture2D SSAO;

void main() {
	//float2 texelSize = 1.0 / float2();
	////avarage size around pixel to remove noisepattern
	//float result = 0;
	//for (int x = -2; x < 2; x++)
	//{
	//	for (int y = -2; y < 2; y++)
	//	{
	//		float2 offset = float2(float(x), float(y))*texelSize;
	//		result += SSAO.Sample()
	//	}
	//}

}